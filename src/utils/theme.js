import { createTheme } from '@material-ui/core/styles';

export const theme = createTheme({
  palette: {
    primary: { main: '#990011FF' }
  }
});

export const loginTheme = createTheme({
  palette: {
    primary: { main: '#FCF6F5FF' },
    secondary: { main: '#990011FF' }
  }
});
